# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.uv/
uv.lock

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Build artifacts (C++ extension)
*.obj
*.pdb
*.ilk
*.exp
*.lib
*.o
build/
out/
x64/
Debug/
Release/

# MSBuild
*.user
*.suo

# Local config / secrets
*.local
.env
.env.local
local-config/
secrets/

# Logs
*.log
logs/

# Test artifacts / dumps
*.dmp
test-output/
crash-dumps/

# Patch rejects
*.rej
*.orig

# OS
desktop.ini

# Local-only working dirs (never commit)
.release-staging/
.test-baseline-backup/

# Per-user real configuration — MUST NOT be committed
# (the example + schema ARE committed; the real file that holds actual
#  paths / snapshot names / guest passwords is not)
driver-harness.config.json

# Runtime artifacts left by installer/doctor (always local, never commit)
installer/.vkd-registry-backup-*.reg
session_snapshots/
